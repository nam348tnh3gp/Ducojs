[package]
name = "libducohasher"
version = "0.1.1"
edition = "2021"

[lib]
name = "libducohasher"  # ← Sửa lại cho khớp với tên thư mục
crate-type = ["cdylib"]

[dependencies]
sha1_smol = "1.0.1"
neon = "0.10"
hex = "0.4"
